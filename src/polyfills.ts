/******************************************************************
 * BROWSER POLYFILLS
 */

// Zone JS is required by Angular itself.
import 'zone.js/dist/zone';  // Included with Angular CLI.

(window as any).__Zone_disable_ErrorStack = true; // Disable ErrorStack
(window as any).__Zone_enable_cross_context_check = true; // Enable cross-context check

// Other polyfills can be added below.

